#!/usr/bin/env bash

# Change directory to the current script's directory
cd "$(dirname "$0")"

# Source the .env file
if [ -f .etc/.barerc ]; then
	source .etc/.barerc
fi

# Temporarily add ./ to PATH
PATH="./:$PATH"

# Delegate the rest
script_name="$1" && shift
./"$script_name" "$@"